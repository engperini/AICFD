# Runtime. Deliberately minimal: the structured-mesh reader in aicfd/foam
# removes the need for VTK or pyvista (ADR-008), and the spec is validated by
# hand rather than by a schema library (ADR-010).
numpy>=1.24
PyYAML>=6.0
