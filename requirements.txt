# Runtime. Deliberately minimal: the structured-mesh reader in aicfd/foam
# removes the need for VTK or pyvista (ADR-008), and the spec is validated by
# hand rather than by a schema library (ADR-010).
numpy>=1.24
PyYAML>=6.0

# The Word deliverable only (`aicfd report`). Everything else -- building a
# case, solving it, the eleven checks, the page -- runs without these, and
# imports them nowhere. Install them when you need the document.
python-docx>=1.1
matplotlib>=3.7
