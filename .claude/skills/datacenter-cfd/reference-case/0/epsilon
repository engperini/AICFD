FoamFile { version 2.0; format ascii; class volScalarField; object epsilon; }
dimensions      [0 2 -3 0 0 0 0];
internalField   uniform 0.01;
boundaryField
{
    fanwall    { type fixedValue; value uniform 0.01; }
    return     { type inletOutlet; inletValue uniform 0.01; value uniform 0.01; }
    floor      { type epsilonWallFunction; value uniform 0.01; }
    ceiling    { type epsilonWallFunction; value uniform 0.01; }
    sidewalls  { type epsilonWallFunction; value uniform 0.01; }
}
