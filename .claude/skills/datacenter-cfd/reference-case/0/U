FoamFile { version 2.0; format ascii; class volVectorField; object U; }
dimensions      [0 1 -1 0 0 0 0];
internalField   uniform (0 0 0);
boundaryField
{
    fanwall
    {
        type            fixedValue;
        value           uniform (1.8 0 0);
    }
    return
    {
        type            pressureInletOutletVelocity;
        value           uniform (0 0 0);
    }
    floor      { type noSlip; }
    ceiling    { type noSlip; }
    sidewalls  { type noSlip; }
}
