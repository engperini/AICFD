# Not the clone's history, the local venv, or anything the tool generates.
# `reference/` IS copied in: the three worked results are what the page shows
# on first run, so a fresh container has something to look at without solving
# anything first (ADR-032).
.git
.venv
runs
results
__pycache__
