# Not the clone's history, the local venv, or solver scratch. `results/` IS
# copied in: the three worked results are what the page shows on first run, so
# a fresh container has something to look at without solving anything first.
.git
.venv
runs
__pycache__
