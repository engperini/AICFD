"""The test suite.

A package rather than a loose folder so `python3 -m unittest discover tests`
and `python3 -m unittest tests.test_model` import these modules under the same
names, and a shared helper can be imported the same way from either.
"""
